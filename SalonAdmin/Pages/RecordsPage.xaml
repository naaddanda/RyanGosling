<Page x:Class="SalonAdmin.Pages.RecordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgBrush}"
      Loaded="Page_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📅 Записи" FontSize="22" FontWeight="SemiBold" />

        <Border Grid.Row="1" Background="White" BorderBrush="#EEE" BorderThickness="1" CornerRadius="14" Padding="14" Margin="0,14,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <DatePicker x:Name="DpDate" SelectedDateChanged="DpDate_SelectedDateChanged"/>
                <Button Grid.Column="1" Content="Сегодня" Margin="10,0,0,0" Style="{StaticResource AccentBtn}" Click="BtnToday_Click"/>

                <ComboBox x:Name="CmbStatus" Grid.Column="3" Height="34" VerticalContentAlignment="Center"
                          SelectionChanged="Filter_SelectionChanged"/>
                <ComboBox x:Name="CmbMaster" Grid.Column="5" Height="34" VerticalContentAlignment="Center"
                          SelectionChanged="Filter_SelectionChanged"/>
            </Grid>
        </Border>

        <DataGrid x:Name="Dg"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9F9F9"
                  Background="White"
                  BorderBrush="#EEE"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Время" Width="90" Binding="{Binding Начало, StringFormat={}{0:HH:mm}}"/>
                <DataGridTextColumn Header="Услуга" Width="2*" Binding="{Binding Услуга}"/>
                <DataGridTextColumn Header="Мастер" Width="*" Binding="{Binding Мастер}"/>
                <DataGridTextColumn Header="Клиент" Width="*" Binding="{Binding Клиент}"/>
                <DataGridTextColumn Header="Статус" Width="*" Binding="{Binding Статус}"/>
                <DataGridTextColumn Header="Сумма" Width="120" Binding="{Binding Итог}"/>

                <DataGridTemplateColumn Header="Действия" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox Height="30"
                                      ItemsSource="{Binding Statuses}"
                                      SelectedItem="{Binding Статус, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      SelectionChanged="InlineStatus_SelectionChanged"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

